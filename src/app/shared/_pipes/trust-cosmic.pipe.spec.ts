import { TrustCosmicPipe } from './trust-cosmic.pipe';

describe('TrustCosmicPipe', () => {
  it('create an instance', () => {
    const pipe = new TrustCosmicPipe();
    expect(pipe).toBeTruthy();
  });
});
